class Queue{
    def __init__(self):
        self.items[]

    def is_empty(self):
        return len (self.items) == 0

    def enqueue(self,item):
        self.item.append(item)
    
    def dequeue(self):
        if len(self.items) == 0
            print("queue is empty")
            return
        x = self.items.pop(0)
        return x

    def front(self):
        if len(self.items) == 0
            print("queue is empty")
            return
        return self.items[0]

    def rear(self):
        if len(self.items) == 0
            print("queue is empty")
            return
        return self.items[-1]
}